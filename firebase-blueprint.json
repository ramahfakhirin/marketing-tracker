{
  "entities": {
    "team": {
      "title": "Team Member",
      "description": "Registered team members and credentials",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "role": { "type": "string" },
        "username": { "type": "string" },
        "password": { "type": "string" }
      },
      "required": ["id", "name", "role", "username"]
    },
    "schools": {
      "title": "School Lead Record",
      "description": "School marketing lead records",
      "properties": {
        "no": { "type": "number" },
        "namaSekolah": { "type": "string" },
        "status": { "type": "string" }
      },
      "required": ["no", "namaSekolah", "status"]
    },
    "custom_database": {
      "title": "Custom Surveyed Database",
      "description": "User surveyed school database by province and city",
      "properties": {
        "provinsi": { "type": "string" },
        "kota": { "type": "string" }
      },
      "required": ["provinsi", "kota"]
    }
  }
}
